[project]
    dependencies = [
        "lxml>=5.4.0",
        "pygithub>=2.6.1",
        "python-dateutil>=2.9.0.post0",
    ]
    description = "Updates the repo's README.md."
    name = "updater"
    requires-python = ">=3.13"
    version = "0.2.0"

[tool.ruff.lint]
    allowed-confusables = ["−"]
    ignore = [
        "D200",
        "D202",
        "D212",
        "EM102",
        "PLR2004",
        "T201",
        "TRY003",
    ]
    select = ["ALL"]

    [tool.ruff.lint.isort]
        combine-as-imports         = true
        force-sort-within-sections = true
        order-by-type              = true
